/**
 * Función para filtrar y procesar datos JSON
 * Por ahora devuelve los datos tal como entran
 * Aquí pondremos toda la lógica de parseo en siguientes pasos
 */
export function filterData(data: unknown): unknown {
  // Por ahora simplemente devuelve la data tal cual

  
  return data;
}

export default filterData;
